var findSumOfOddElements = function(head) {
    

    let temp = head;
  let sum = 0;

  while (temp !== null) {
    if (temp.val % 2 !== 0) {
      sum += temp.val;
    }
    temp = temp.next;
  }

  return sum;

	// return the sum of odd values of the linkedlist
};
